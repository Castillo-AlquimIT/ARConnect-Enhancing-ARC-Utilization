name: testapp
description: A new Flutter project

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  syncfusion_flutter_gauges: ^33.2.5
  google_generative_ai: ^0.4.6  # ← latest stable on 0.4.x
  table_calendar: ^3.0.8
  flutter_dotenv: ^5.0.2
  flutter_appauth: ^8.0.3
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0